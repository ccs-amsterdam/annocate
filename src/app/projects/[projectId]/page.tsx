"use client";

export default function Job({ params }: { params: { projectId: number } }) {
  return (
    <div className="mx-auto mt-10 flex max-w-xl flex-col gap-2 p-3">
      <p>Put some general project info here, and some instructions for where to go next</p>
    </div>
  );
}
