// @annotinder/contracts
// Phase 1 will populate this with the full API contract (zod schemas for
// job/codebook/unit/coder/variables endpoints). For now this is a scaffolding
// placeholder so the workspace builds end-to-end.
export const CONTRACTS_VERSION = "0.0.0";
